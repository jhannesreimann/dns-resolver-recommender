# Deploy

Server configuration and setup scripts for the Hetzner VM at 46.225.184.21.

Planned contents:
  - nginx.conf         Nginx reverse proxy config (HTTPS, Let's Encrypt)
  - setup.sh           Initial server setup script
  - dns-server.service systemd unit for the custom DNS server
  - certbot notes      SSL certificate renewal setup
