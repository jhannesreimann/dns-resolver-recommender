# Copy to /etc/dnsrr/dnsrr.env on the VM, fill in real values, chmod 600.
# Never commit the real file.

CLOUDFLARE_API_KEY=replace-me
ZONE_ID=replace-me
ZONE_DOMAIN=diic-hpi.org
ROTATION_TARGET_IP=127.0.0.1
ROTATION_TTL_SECONDS=60
ROTATION_RECORD_COMMENT=dnsrr auto-generated, safe to delete
CORS_ORIGINS=https://dns.diic-hpi.org
