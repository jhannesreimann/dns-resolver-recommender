# Selenium Headless Probe

This is a headless probe to execute the measurements on https://dns.diic-hpi.org.

Execution via:

```bash
./run-measurement.sh
```

Dependencies:
- `podman` or `docker` (change in `run-measurement.sh` to liking)
