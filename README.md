# DNS Resolver Recommender

## Running

To get a development shell:

```bash
# Build a development shell that holds necessary dependencies
nix develop
```

Building the Rust source code (in the development shell):

```bash
build
```

Then run the web server for pulling up the website

```bash
server
```

Go to http://localhost:8000 to see the resulting website.
